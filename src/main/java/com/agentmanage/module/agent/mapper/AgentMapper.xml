<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentmanage.module.agent.mapper.AgentMapper">

    <resultMap id="agentPoMap" type="com.agentmanage.module.agent.entity.AgentPo">
        <id column="ID" property="id" jdbcType="INTEGER"/>
        <result column="MOBILE_NO" property="mobileNo" jdbcType="VARCHAR"/>
        <result column="PASSWORD" property="password" jdbcType="VARCHAR"/>
        <result column="SALT" property="salt" jdbcType="VARCHAR"/>
        <result column="GMT_CREATED" property="gmtCreated" jdbcType="TIMESTAMP"/>
        <result column="GMT_MODIFIED" property="gmtModified" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="allColumns">
        id, mobile_no, password, salt, real_name, gmt_created, gmt_modified
    </sql>

    <insert id="insert">
        insert into tb_agent(mobile_no, password, salt, real_name, gmt_created, gmt_modified)
        values(
        #{mobileNo, jdbcType=VARCHAR},
        #{password, jdbcType=VARCHAR},
        #{salt, jdbcType=VARCHAR},
        #{realName, jdbcType=VARCHAR},
        now(),
        now()
        )
    </insert>
    
    <update id="update">
        update tb_agent set
        password = #{password, jdbcType=VARCHAR},
        real_name = #{realName, jdbcType=VARCHAR},
        gmt_modified = now()
        where id = #{id, jdbcType=INTEGER}
    </update>

    <select id="selectByMobileNo" resultMap="agentPoMap">
        SELECT
          <include refid="allColumns" />
        from tb_agent
        where mobile_no=#{mobileNo, jdbcType=VARCHAR}
    </select>
</mapper>